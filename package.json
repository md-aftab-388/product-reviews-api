{
  "name": "product-reviews-api",
  "version": "1.0.0",
  "description": "RESTful API for managing product reviews with PostgreSQL database",
  "main": "app.js",
  "scripts": {
    "prestart": "node scripts/prestart.js",
    "start": "nodemon app.js",
    "dev": "nodemon app.js",
    "migrate": "node scripts/migrate.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
